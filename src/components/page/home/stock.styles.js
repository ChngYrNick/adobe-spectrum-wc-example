import {css} from 'lit';

export default css`
  :host {
    display: flex;
    border-top: 1px solid var(--spectrum-global-color-gray-200);
    flex-wrap: wrap;
    margin-top: 16px;
    padding-top: 16px;
  }
`;
