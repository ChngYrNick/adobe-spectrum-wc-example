import {css} from 'lit';

export default css`
  :host {
    display: flex;
    margin-left: 16px;
    padding-left: 16px;
    border-left: 1px solid var(--spectrum-global-color-gray-200);
    flex-wrap: wrap;
  }
`;
